[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
board_build.partitions = default_4MB.csv  # Hapus baris ini atau ganti dengan:
board_build.partitions = min_spiffs.csv   # Alternatif yang valid
